{
  "name": "router",
  "productName": "Router",
  "version": "0.0.1",
  "description": "Review your Claude + Codex work and post it to the Router cohort feed — all on your machine, on your Claude subscription.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "collect": "node src/transcripts.js",
    "eval": "node evals/run.js",
    "eval:fixtures": "node evals/run.js --fixtures"
  },
  "author": "James",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
